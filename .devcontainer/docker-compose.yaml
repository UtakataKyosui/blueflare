services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspaces/sandbox:cached
    command: sleep infinity
    security_opt:
      - no-new-privileges:false
    deploy:
      resources:
        limits:
          memory: 8g
        reservations:
          memory: 6g
